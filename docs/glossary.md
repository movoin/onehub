# 名词概念

## 服务

`服务` 为具体的实例，用于隔离不同业务的数据，除了服务名称外，还定义了具体的自定义结构。

------


## 服务引擎

实现有 `日志`、`跟踪`、`统计` 三款引擎。引擎负责定义数据的操作具体功能、逻辑的实现，并通过 `中间语法` 向 `存储后端` 发送具体数据存取指定。

------


## 存储后端

统一使用 `JSON` 实现的一套 `类 SQL` 的强约定数据查询语法，并实现有 `MySQL`、`MongoDB`、`ElasticSearch` 作为存储实例。